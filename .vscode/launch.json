{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run agent.py",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/agent.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}